# QA 检查记录

本修复版执行了以下检查：

1. JavaScript 语法检查：`node --check game.js`
2. 数据一致性检查：
   - 场景 `goto` 目标存在
   - 场景引用的对话存在
   - 行动和对话引用的证据存在
   - 组合推理引用的证据存在
   - 角色立绘和场景素材路径存在
3. DOM 级烟雾测试：
   - 初始化渲染
   - 数据面板
   - 音频包面板
   - 存档管理
   - 案件时间线
   - 章节回放
   - 城市地图
   - 证据组合
   - 视觉图鉴
   - 角色档案
   - 成就与结局收藏
4. 修复项：
   - 手动读档改为使用 `hydrate()`，避免旧槽位字段缺失导致后续 UI 或音频设置异常。
   - 时间线对 `hub` 场景显示为“推理中枢”，不再显示“未命名案件”。
   - 存档卡片对旧槽位缺失 `activeCase` 的情况增加兜底逻辑。
